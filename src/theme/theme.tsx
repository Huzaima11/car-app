import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1976d2",
    },
    error: {
      main: "#FF0000",
    },
    common: {
      black: "#000000",
    },
  },
});

export default theme;
